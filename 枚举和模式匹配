定义枚举
什么是枚举？
任何 IP 地址都可以是版本 4 或版本 6 地址，但不能同时是两者。IP 地址的这一特性使得枚举数据结构非常合适，因为枚举值只能是其变体之一。版本 4 和版本 6 地址本质上仍然是 IP 地址，
因此在代码处理适用于任何类型 IP 地址的情况时，应将它们视为同一类型。
比如如下代码：
#[derive(Debug)]
struct Ipv4Addr<'a>{
    name:&'a str,
    address:&'a str,

}
#[derive(Debug)]
struct Ipv6Addr{
    name:String,
    address:String,

}
#[derive(Debug)]
enum Address<'a>{
    v4(Ipv4Addr<'a>),
    v6(Ipv6Addr),
}

fn main(){
    let home = Address::v4(Ipv4Addr { name: "home", address: "127.0.0.1" });
    let loopback = Address::v6(Ipv6Addr { name: String::from("looopback"), address: String::from("::1")});
    println!("{:?}",home);
    // 使用了 {:?} 或 {:#?} 来打印 home，但 Address、Ipv4Addr 和 Ipv6Sddr 没有实现 Debug trait。
    // Rust 要求任何使用 {:?} 打印的类型必须实现 Debug。
    println!("{:?}",loopback);

}

上面代码在内存中分配如下：
在static area中将main函数中home等值存入（存入static是因为有生命周期），Ipv4addr使用借用的方式，
在stack中存入home和127.0.0.1的地址，Address的V4在stack中指向Ipv4addr,loopback和::1因为在Ipv6addr
中存放方式是String，所以从static中复制一份在堆hoop中（包含::1等值），并且static中的::1
不会被释放，栈中的Ipv6addr指向堆中的地址，最后Address的V6指向栈中的Ipv6addr，具体图如下：

【储藏室（静态存储区）】（程序启动就放好，永不释放）
+-----------------------------------+
| "home"        | h | o | m | e |   |  <- 地址 1000，长度 4
|-----------------------------------|
| "127.0.0.1"   | 1 | 2 | 7 | . | 0 | . | 0 | . | 1 |  <- 地址 1010，长度 9
|-----------------------------------|
| "looopback"   | l | o | o | o | p | b | a | c | k |  <- 地址 1020，长度 9
|-----------------------------------|
| "::1"         | : | : | 1 |       |  <- 地址 1030，长度 3
+-----------------------------------+

【货架（堆内存）】（String 复制文字，放包裹）
+-----------------------------------+
| looopback     | l | o | o | o | p | b | a | c | k |  <- 地址 5000，长度 9，容量 9
|-----------------------------------|
| ::1           | : | : | 1 |       |  <- 地址 5100，长度 3，容量 3
+-----------------------------------+

【工作台（栈内存）】（临时盒子，main 结束清理）
+-----------------------------------+
| home (Address::v4, 40 字节)       |
| +-------------------------------+ |
| | 标签: v4                     | |  <- 1 字节
| |-----------------------------| |
| | Ipv4Addr (32 字节)          | |
| | +-------------------------+ | |
| | | name: &str             | | |  <- 16 字节
| | | 地址: 1000 ("home")    | | |  --> 储藏室 "home"
| | | 长度: 4                | | |
| | +-------------------------+ | |
| | +-------------------------+ | |
| | | address: &str          | | |  <- 16 字节
| | | 地址: 1010 ("127.0.0.1") | | --> 储藏室 "127.0.0.1"
| | | 长度: 9                | | |
| | +-------------------------+ | |
| +-----------------------------+ |
| [填充: 7 字节]                 |
+-----------------------------------+
| loopback (Address::v6, 56 字节)   |
| +-------------------------------+ |
| | 标签: v6                     | |  <- 1 字节
| |-----------------------------| |
| | Ipv6Addr (48 字节)          | |
| | +-------------------------+ | |
| | | name: String           | | |  <- 24 字节
| | | 地址: 5000 (looopback) | | |  --> 货架 "looopback"
| | | 长度: 9                | | |
| | | 容量: 9                | | |
| | +-------------------------+ | |
| | +-------------------------+ | |
| | | address: String        | | |  <- 24 字节
| | | 地址: 5100 (::1)       | | |  --> 货架 "::1"
| | | 长度: 3                | | |
| | | 容量: 3                | | |
| | +-------------------------+ | |
| +-----------------------------+ |
| [填充: 7 字节]                 |
+-----------------------------------+




